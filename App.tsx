// Root-level App entry for EAS builds running from repo root.
// Expo's default entry (`node_modules/expo/AppEntry.js`) imports `../../App`.
// In this monorepo, the real app lives in `mobile/App.tsx`.

export { default } from './mobile/App';



