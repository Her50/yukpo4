# Analyse des logs backend - 2025-11-28 18:38

## Vue d'ensemble
Période analysée : 18:38:23 - 18:39:00 (37 secondes)
Niveau de log : DEBUG (très détaillé)

## 1. Monitoring et Health Checks

### 1.1 Pipeline Health Worker
**Fréquence** : Toutes les ~15 secondes (18:38:23, 18:38:38, 18:38:53)

**Requêtes exécutées** :
- `SELECT status, COUNT(*) FROM video_generation_jobs GROUP BY status` (~1.8ms)
- `SELECT COUNT(*) FROM video_generation_jobs WHERE status = 'failed' AND updated_at >= NOW() - INTERVAL '24 hours'` (~1.6ms)
- `SELECT COUNT(*) FROM video_generation_jobs WHERE status = 'completed' AND updated_at >= NOW() - INTERVAL '24 hours'` (~1.7ms)
- `SELECT MAX(updated_at) FROM video_generation_jobs WHERE status = 'completed'` (~1.7ms)
- `SELECT job_id, status, updated_at FROM video_generation_jobs WHERE status IN ('queued', 'running') AND updated_at < NOW() - INTERVAL '30 minutes'` (~2.3ms)
- `SELECT COUNT(*) FROM media WHERE media_type = 'video' AND uploaded_at >= NOW() - ($1::int * INTERVAL '1 day')` (~2.5ms)
- `SELECT COUNT(*) FILTER (WHERE event_type = 'view') AS views, COUNT(*) FILTER (WHERE event_type = 'share') AS shares, COALESCE(AVG((metadata ->> 'quality_score')::float), 0.0) AS avg_quality FROM media_engagement` (~1.9ms)
- `SELECT COUNT(*) FILTER (WHERE status = 'completed') AS completed, COUNT(*) FILTER (WHERE status IN ('scheduled', 'processing')) AS pending FROM media_distribution` (~1.7ms)

**Observations** :
- ✅ Toutes les requêtes retournent 0 résultat (pas de jobs en cours)
- ✅ Temps d'exécution très rapides (< 3ms)
- ✅ Aucun job bloqué détecté

### 1.2 DB Health Monitor
**Fréquence** : Toutes les 30 secondes (18:38:30, 18:39:00)

**Statut** :
```
[DB Monitor] ✅ Pool healthy - Size: 10, Active: 0, Idle: 10
```

**Requête** : `SELECT 1` (~0.7ms)

**Observations** :
- ✅ Pool de connexions sain
- ✅ 10 connexions disponibles, toutes inactives
- ✅ Pas de connexions actives (pas de charge)

## 2. Workers Background

### 2.1 Global Promo Scheduler
**Fréquence** : Toutes les 30 secondes (18:38:30, 18:39:00)

**Requêtes exécutées** :
1. **Flash Sales Scheduled** :
   ```sql
   SELECT lfs.id, lfs.live_session_id, lfs.service_id, lfs.promo_price_cfa,
          lfs.start_at, lfs.scheduled_notification_sent_at,
          ls.host_user_id, ls.service_id AS primary_service_id,
          ls.metadata, ls.title
   FROM live_flash_sales lfs
   JOIN live_sessions ls ON ls.id = lfs.live_session_id
   WHERE lfs.status = 'scheduled'
     AND lfs.start_at <= $1
     AND lfs.scheduled_notification_sent_at IS NULL
   ```
   - Temps : ~1.9ms
   - Résultats : 0

2. **Global Promo Events Scheduled** :
   ```sql
   SELECT id, display_name
   FROM global_promo_events
   WHERE status = 'scheduled' AND starts_at <= $1
   ```
   - Temps : ~2.2ms
   - Résultats : 0

3. **Social Publication Jobs** :
   ```sql
   SELECT id, media_id, platform, payload, status, attempt, last_error,
          scheduled_for, created_at, updated_at
   FROM social_publication_jobs
   WHERE status = 'queued' AND scheduled_for <= NOW()
   ORDER BY scheduled_for ASC
   LIMIT $1
   ```
   - Temps : ~2.2ms
   - Résultats : 0

4. **Flash Sales Live** :
   ```sql
   SELECT lfs.id, lfs.live_session_id, lfs.service_id, lfs.promo_price_cfa,
          ls.host_user_id, ls.service_id AS primary_service_id,
          ls.metadata, ls.title
   FROM live_flash_sales lfs
   JOIN live_sessions ls ON ls.id = lfs.live_session_id
   WHERE lfs.status = 'scheduled' AND lfs.start_at <= $1
   ```
   - Temps : ~1.6ms
   - Résultats : 0

5. **Global Promo Events Ending** :
   ```sql
   SELECT id, display_name
   FROM global_promo_events
   WHERE status IN ('scheduled','live') AND ends_at <= $1
   ```
   - Temps : ~1.8ms
   - Résultats : 0

6. **Flash Sales Ending** :
   ```sql
   SELECT lfs.id, lfs.live_session_id, lfs.service_id, lfs.end_at,
          lfs.ending_notification_sent_at,
          ls.host_user_id, ls.service_id AS primary_service_id,
          ls.metadata, ls.title
   FROM live_flash_sales lfs
   JOIN live_sessions ls ON ls.id = lfs.live_session_id
   WHERE lfs.status = 'live'
     AND lfs.end_at <= $1
     AND lfs.ending_notification_sent_at IS NULL
   ```
   - Temps : ~2.1ms
   - Résultats : 0

7. **Global Promo Entries Live** :
   ```sql
   SELECT e.id AS entry_id, e.service_id, e.submitted_by_user_id,
          ev.display_name AS event_display_name
   FROM global_promo_entries e
   JOIN global_promo_events ev ON ev.id = e.event_id
   WHERE ev.status = 'live' AND e.status = 'approved'
   ```
   - Temps : ~2.2ms
   - Résultats : 0

8. **Global Promo Entries Archived** :
   ```sql
   SELECT e.id AS entry_id, e.service_id, e.submitted_by_user_id,
          ev.display_name AS event_display_name
   FROM global_promo_entries e
   JOIN global_promo_events ev ON ev.id = e.event_id
   WHERE ev.status = 'archived' AND e.status <> 'ended'
   ```
   - Temps : ~1.9ms
   - Résultats : 0

9. **Flash Sales AI Commentary** :
   ```sql
   WITH reservations AS (
       SELECT flash_sale_id, SUM(quantity) AS reserved_quantity
       FROM live_flash_sale_reservations
       GROUP BY flash_sale_id
   )
   SELECT lfs.id, lfs.live_session_id, lfs.service_id, lfs.stock_target,
          COALESCE(res.reserved_quantity, 0) AS reserved_quantity,
          lfs.commentary_interval_seconds, lfs.last_commentary_sent_at,
          lfs.end_at, COALESCE(lfs.metadata, '{}'::jsonb) AS flash_metadata,
          ls.host_user_id, ls.service_id AS primary_service_id,
          COALESCE(ls.metadata, '{}'::jsonb) AS session_metadata, ls.title
   FROM live_flash_sales lfs
   JOIN live_sessions ls ON ls.id = lfs.live_session_id
   LEFT JOIN reservations res ON res.flash_sale_id = lfs.id
   WHERE lfs.status = 'live'
     AND lfs.commentary_mode = 'ai_voice'
     AND lfs.end_at > $1
     AND (lfs.last_commentary_sent_at IS NULL
          OR lfs.last_commentary_sent_at <= $1 - make_interval(secs => lfs.commentary_interval_seconds))
   ```
   - Temps : ~8.6ms (la plus lente, mais acceptable)
   - Résultats : 0

10. **Update Flash Sales Ended** :
    ```sql
    UPDATE live_flash_sales SET status = 'ended', updated_at = NOW()
    WHERE status IN ('scheduled', 'live') AND end_at <= $1
    ```
    - Temps : ~3.8ms
    - Rows affected : 0

**Observations** :
- ✅ Toutes les requêtes retournent 0 résultat (pas d'événements actifs)
- ✅ Temps d'exécution excellents (< 9ms)
- ✅ Worker fonctionne correctement, vérifie tous les cas

### 2.2 Delivery Matching Worker
**Fréquence** : Toutes les 30 secondes (18:38:30, 18:39:00)

**Requête** :
```sql
SELECT id, delivery_id, zone_id, status, priority, attempt_count,
       payload, next_attempt_at, enqueued_at, updated_at
FROM delivery_matching_queue
WHERE status IN ('queued', 'searching')
  AND next_attempt_at <= NOW()
ORDER BY priority ASC, next_attempt_at ASC
LIMIT $1
```

**Statut** :
```
[DeliveryMatchingWorker] Aucune livraison à traiter (batch = 10)
```

**Observations** :
- ✅ Temps d'exécution : ~7.3ms (première fois), ~0.9ms (seconde fois)
- ✅ Aucune livraison en attente
- ✅ Worker fonctionne correctement

### 2.3 Order Timeout Monitor
**Fréquence** : Toutes les 30 secondes (18:38:30)

**Requêtes exécutées** :
1. **Product Orders Pending** :
   ```sql
   SELECT id, service_id, product_index, client_user_id, provider_user_id
   FROM product_orders
   WHERE status = 'pending'
     AND validation_deadline IS NOT NULL
     AND validation_deadline <= $1
   LIMIT 50
   ```
   - Temps : ~2.1ms
   - Résultats : 0

2. **Delivery Proximity Suggestions** :
   ```sql
   SELECT delivery_id, suggested_status, created_at, auto_confirm_after_seconds
   FROM delivery_proximity_suggestions
   WHERE status = 'pending'
     AND auto_confirm_after_seconds IS NOT NULL
     AND created_at + (auto_confirm_after_seconds || ' seconds')::interval <= NOW()
   LIMIT 50
   ```
   - Temps : ~2.0ms
   - Résultats : 0

**Observations** :
- ✅ Aucune commande en timeout
- ✅ Temps d'exécution rapides

### 2.4 Delivery Timeout Monitor
**Fréquence** : Toutes les 30 secondes (18:38:30)

**Requêtes exécutées** (3 fois avec différents statuts) :
```sql
SELECT d.id, d.status::text as status, d.updated_at,
       d.creator_id, d.recipient_user_id, c.user_id as courier_user_id
FROM deliveries d
LEFT JOIN couriers c ON c.id = d.courier_id
WHERE d.status::text = $1
  AND d.updated_at < NOW() - ($2 || ' minutes')::interval
  AND d.status::text != 'delivered'
  AND d.status::text != 'cancelled'
  AND d.status::text != 'completed'
LIMIT 20
```

**Temps d'exécution** :
- Première : ~2.5ms
- Deuxième : ~0.9ms
- Troisième : ~2.6ms

**Résultats** : 0 pour toutes

**Observations** :
- ✅ Aucune livraison en timeout
- ✅ Vérifie plusieurs statuts différents

## 3. Connexions HTTP Externes

### 3.1 Connexion à Remotion Worker
**Timestamp** : 18:38:40.703

**Détails** :
- URL : `http://46.224.14.85:7880/`
- Connexion établie : ✅
- Bytes envoyés : 488
- Headers parsés : 4
- Body reçu : 12 bytes
- Connexion réutilisée dans le pool

**Observations** :
- ✅ Connexion réussie
- ✅ Pool de connexions HTTP fonctionne
- ✅ Pas d'erreur de timeout

## 4. Métriques de Performance

### 4.1 Temps d'exécution des requêtes SQL

| Type de requête | Temps moyen | Temps min | Temps max |
|----------------|-------------|-----------|-----------|
| Health checks simples | ~0.7ms | 0.7ms | 0.8ms |
| COUNT queries | ~1.7ms | 0.7ms | 2.5ms |
| SELECT simples | ~1.8ms | 0.8ms | 2.6ms |
| SELECT avec JOIN | ~2.0ms | 1.5ms | 2.2ms |
| SELECT avec CTE | ~8.6ms | 1.0ms | 8.6ms |
| UPDATE | ~3.8ms | 0.8ms | 3.8ms |

**Analyse** :
- ✅ Performance excellente globalement
- ⚠️ La requête CTE (Flash Sales AI Commentary) est la plus lente (~8.6ms) mais reste acceptable
- ✅ Pas de requête lente (> 100ms)

### 4.2 Fréquence des Workers

| Worker | Intervalle | Observations |
|--------|------------|-------------|
| Pipeline Health | ~15s | ✅ Régulier |
| DB Health Monitor | 30s | ✅ Régulier |
| Global Promo Scheduler | 30s | ✅ Régulier |
| Delivery Matching | 30s | ✅ Régulier |
| Order Timeout Monitor | 30s | ✅ Régulier |
| Delivery Timeout Monitor | 30s | ✅ Régulier |

**Analyse** :
- ✅ Tous les workers fonctionnent à la fréquence attendue
- ✅ Pas de dérive temporelle

## 5. Points d'Attention

### 5.1 Points Positifs ✅
1. **Performance** : Toutes les requêtes sont très rapides (< 10ms)
2. **Stabilité** : Aucune erreur détectée
3. **Monitoring** : Tous les workers fonctionnent correctement
4. **Pool de connexions** : Sain (10 connexions, toutes disponibles)
5. **Charge** : Faible (0 connexions actives)

### 5.2 Points à Surveiller ⚠️
1. **Requête CTE Flash Sales** : La plus lente (~8.6ms), mais acceptable
   - Suggestion : Vérifier les index sur `live_flash_sales.status`, `live_flash_sales.commentary_mode`, `live_flash_sales.end_at`
2. **Aucune activité** : Tous les workers retournent 0 résultat
   - Normal si pas d'activité utilisateur
   - À surveiller si activité attendue

### 5.3 Recommandations 🔧
1. **Optimisation requête CTE** :
   - Ajouter index composite sur `(status, commentary_mode, end_at)` si pas déjà présent
   - Vérifier EXPLAIN ANALYZE pour identifier les bottlenecks

2. **Monitoring** :
   - Ajouter des alertes si temps d'exécution > 100ms
   - Surveiller le nombre de connexions actives

3. **Logs** :
   - Le niveau DEBUG est très verbeux
   - Considérer INFO pour production si pas de problème

## 6. Conclusion

**État général** : ✅ **EXCELLENT**

- Système stable et performant
- Tous les workers fonctionnent correctement
- Performance de base de données excellente
- Aucune erreur détectée
- Pool de connexions sain

**Action requise** : Aucune action immédiate nécessaire. Continuer le monitoring régulier.

