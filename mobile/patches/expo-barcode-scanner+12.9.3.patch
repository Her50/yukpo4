diff --git a/node_modules/expo-barcode-scanner/android/build.gradle b/node_modules/expo-barcode-scanner/android/build.gradle
index fe9c910..c4a0dbc 100644
--- a/node_modules/expo-barcode-scanner/android/build.gradle
+++ b/node_modules/expo-barcode-scanner/android/build.gradle
@@ -25,7 +25,7 @@ buildscript {
   // Ensures backward compatibility
   ext.getKotlinVersion = {
     if (ext.has("kotlinVersion")) {
-      ext.kotlinVersion()
+      ext.get("kotlinVersion")
     } else {
       ext.safeExtGet("kotlinVersion", "1.8.10")
     }
@@ -59,9 +59,12 @@ if (!safeExtGet("expoProvidesDefaultConfig", false)) {
 }
 
 android {
+  // ✅ FIX: Définir compileSdkVersion directement AVANT toute condition
+  // Gradle exige que compileSdkVersion soit défini, même dans une condition
+  compileSdkVersion safeExtGet("compileSdkVersion", 35)
+  
   // Remove this if and it's contents, when support for SDK49 is dropped
   if (!safeExtGet("expoProvidesDefaultConfig", false)) {
-    compileSdkVersion safeExtGet("compileSdkVersion", 34)
 
     defaultConfig {
       minSdkVersion safeExtGet("minSdkVersion", 23)
