# 📋 Liste des fichiers avec HTML non utilisés

Ces fichiers contiennent du HTML/CSS web mais **ne sont PAS utilisés** par l'application mobile.

## Fichiers à ignorer (non utilisés)

1. `TestimonialsAndPartners.tsx` - `<section>`, `<img>`, classes CSS
2. `Sidebar.tsx` - `<aside>`, classes CSS, `useLocation` (n'existe pas)
3. `SchedulerStatusCard.tsx` - Classes CSS web
4. `RolePlanNotice.tsx` - Classes CSS web
5. `OutilsSection.tsx` - `<section>`, import CSS module
6. `MenuAuto.tsx` - `<nav>`, `<a>`, `ROUTES` non défini
7. `MatchSection.tsx` - `<section>`, import CSS module
8. `MainMenu.tsx` - `<nav>`, `react-router-dom` (web)
9. `Loader.tsx` - Classes CSS web
10. `FloatingHelpButton.tsx` - `<a>`, classes CSS
11. `ExportShareCard.tsx` - `<select>`, `<option>`, `<a>`
12. `DynamicMenu.tsx` - `<section>`, `<a>`, `ROUTES_CONFIG` non défini
13. `DesktopMenu.tsx` - `<nav>`, `<ul>`, `<li>`, `useLocation`
14. `CreationSection.tsx` - `<section>`, import CSS module
15. `CardService.tsx` - `<a>`, `ROUTES` non défini

## Action recommandée

Ces fichiers peuvent être :
- **Supprimés** si inutiles
- **Conservés** si prévus pour usage futur (à corriger avant utilisation)
- **Déplacés** dans `legacy/` ou `unused/` pour référence

