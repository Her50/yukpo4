# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
# ✅ CORRIGÉ: Augmentation de la mémoire pour éviter OutOfMemoryError lors du build
org.gradle.jvmargs=-Xmx6144m -XX:MaxMetaspaceSize=1024m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# ✅ CORRIGÉ: Activation du mode parallèle pour accélérer le build
org.gradle.parallel=true
# ✅ CORRIGÉ: Activation du daemon pour améliorer les performances
org.gradle.daemon=true
# ✅ CORRIGÉ: Configuration du nombre de workers pour optimiser le build
org.gradle.workers.max=4

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86_64
# ✅ OPTIMISÉ: ARM seulement (95% des devices Android) pour réduire la taille du build
reactNativeArchitectures=armeabi-v7a,arm64-v8a

# ✅ OPTIMISATION TAILLE BUILD: Activer ProGuard et shrinkResources
android.enableProguardInReleaseBuilds=true
android.enableShrinkResourcesInReleaseBuilds=true

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
newArchEnabled=false

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Enable GIF support in React Native images (~200 B increase)
expo.gif.enabled=true
# Enable webp support in React Native images (~85 KB increase)
expo.webp.enabled=true
# Enable animated webp support (~3.4 MB increase)
# Disabled by default because iOS doesn't support animated webp
expo.webp.animated=false

# Enable network inspector
EX_DEV_CLIENT_NETWORK_INSPECTOR=true

# Use legacy packaging to compress native libraries in the resulting APK.
expo.useLegacyPackaging=false

# ✅ CORRIGÉ 2025-12-11: Options pour améliorer la résilience des téléchargements Gradle
# Désactiver la validation stricte de l'URL de distribution (en cas de problème réseau)
org.gradle.wrapper.validateDistributionUrl=false
# Augmenter le timeout pour les opérations réseau
org.gradle.daemon.idletimeout=10800000
# Activer le cache Gradle pour éviter les téléchargements répétés
org.gradle.caching=true

# ✅ CORRIGÉ: Version Kotlin explicite pour éviter les problèmes de résolution
android.kotlinVersion=1.9.25
# ✅ CORRIGÉ: Définir kotlinVersion comme propriété globale pour qu'elle soit accessible dans les projets inclus (expo-modules-core)
kotlinVersion=1.9.25

# ✅ CORRIGÉ: Configuration des repositories pour éviter les timeouts avec les repositories internes
# Forcer l'utilisation des repositories publics en priorité
org.gradle.dependency.verification.mode=warn
# Désactiver les repositories Maven internes qui peuvent causer des timeouts
systemProp.org.gradle.internal.repository.max.retries=3
systemProp.org.gradle.internal.repository.initial.backoff=1000

