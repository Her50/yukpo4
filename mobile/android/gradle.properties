# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
# Increased for large React Native projects with many dependencies
org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G

android.useAndroidX=true
# Jetifier disabled to prevent Java heap space issues during build
# All dependencies already use AndroidX
android.enableJetifier=false

newArchEnabled=false

hermesEnabled=true

# Force offline mode to bypass JitPack
org.gradle.offline=false

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# Disabled to prevent memory issues during Jetify transformation
org.gradle.parallel=false
org.gradle.configureondemand=true
org.gradle.daemon=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86_64
# Limité à arm64-v8a pour réduire la taille du build (~50% de réduction)
# arm64-v8a couvre 99% des appareils Android modernes (2014+)
reactNativeArchitectures=arm64-v8a

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
newArchEnabled=false

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Enable GIF support in React Native images (~200 B increase)
expo.gif.enabled=true
# Enable webp support in React Native images (~85 KB increase)
expo.webp.enabled=true
# Enable animated webp support (~3.4 MB increase)
# Disabled by default because iOS doesn't support animated webp
expo.webp.animated=false

# Enable network inspector
EX_DEV_CLIENT_NETWORK_INSPECTOR=true

# Use legacy packaging to compress native libraries in the resulting APK.
expo.useLegacyPackaging=false

android.minSdkVersion=24
android.compileSdkVersion=35
android.targetSdkVersion=35
android.buildToolsVersion=35.0.0
android.kotlinVersion=1.9.25
android.packagingOptions.pickFirsts=**/libc++_shared.so,**/libfbjni.so
android.enableProguardInReleaseBuilds=true
android.enableShrinkResourcesInReleaseBuilds=true
android.extraMavenRepos=[]

# Network timeout configurations for Maven dependencies
# Increased timeouts to avoid build failures with slow repositories
systemProp.org.gradle.internal.http.connectionTimeout=900000
systemProp.org.gradle.internal.http.socketTimeout=900000
systemProp.org.gradle.internal.http.requestTimeout=900000

# Maven repository timeouts - increased to 15 minutes
systemProp.http.connectionTimeout=900000
systemProp.http.socketTimeout=900000
systemProp.https.connectionTimeout=900000
systemProp.https.socketTimeout=900000

# JitPack specific timeout - increased to 15 minutes
systemProp.jitpack.timeout=900000

# Gradle daemon timeout - increased for large builds
org.gradle.daemon.idletimeout=3600000

# Build cache optimization
org.gradle.caching=true
org.gradle.cache.cleanup.enabled=true
org.gradle.cache.maxAge=7 days
org.gradle.cache.removeUnusedEntries=true