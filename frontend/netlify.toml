[build]
  publish = "dist-bourse"
  command = "npm install && npx vite build --config vite.bourse.config.ts"

[build.environment]
  VITE_API_BASE_URL = ""
  VITE_APP_ENV = "production"
  VITE_APP_DEBUG = "false"

# Backend cible des redirects :
# ✅ ACTIF — 2026-05-16 : Cloudflare proxy → Fly (https://api.yukpomnang.com)
#    Cloudflare protège avec : WAF Managed Free Ruleset, DDoS L7 mitigation,
#    SSL Full Strict, rate-limit anti-brute-force /api/auth/* (3 req/10s/IP),
#    bot management, hides Fly origin IP.
# 💤 Fly direct — fallback sans CF (https://yukpo-fly-backend.fly.dev)
#    Si Cloudflare a un incident, swap activement vers cette ligne.
# 💤 PRINCIPAL — GCP Cloud Run (https://yukpo-backend-376093909298.europe-west1.run.app)
#    Backend long terme. Réactiver quand GCP repart.

[[redirects]]
  from = "/api/*"
  to = "https://api.yukpomnang.com/api/:splat"
  # to = "https://yukpo-fly-backend.fly.dev/api/:splat"  # 💤 Fly direct (sans CF)
  # to = "https://yukpo-backend-376093909298.europe-west1.run.app/api/:splat"  # 💤 GCP (principal)
  status = 200
  force = true
  # ✅ 2026-05-21 — CORS supprimé : la requête est same-origin via Netlify proxy.
  # Garder Access-Control-Allow-Origin = "*" briserait la nouvelle auth cookie
  # httpOnly (`withCredentials: true` refuse les wildcards d'origine).

[[redirects]]
  from = "/auth/*"
  to = "https://api.yukpomnang.com/auth/:splat"
  # to = "https://yukpo-fly-backend.fly.dev/auth/:splat"  # 💤 Fly direct (sans CF)
  # to = "https://yukpo-backend-376093909298.europe-west1.run.app/auth/:splat"  # 💤 GCP (principal)
  status = 200
  force = true
  # ✅ 2026-05-21 — Cf. /api/*. Pas de CORS wildcard avec withCredentials.

[[redirects]]
  from = "/services/*"
  to = "https://api.yukpomnang.com/services/:splat"
  # to = "https://yukpo-fly-backend.fly.dev/services/:splat"  # 💤 Fly direct (sans CF)
  # to = "https://yukpo-backend-376093909298.europe-west1.run.app/services/:splat"  # 💤 GCP (principal)
  status = 200
  force = true
  # ✅ 2026-05-21 — CORS supprimé (same-origin via proxy, incompatible cookie httpOnly).

[[redirects]]
  from = "/healthz"
  to = "https://api.yukpomnang.com/healthz"
  # to = "https://yukpo-fly-backend.fly.dev/healthz"  # 💤 Fly direct (sans CF)
  # to = "https://yukpo-backend-376093909298.europe-west1.run.app/healthz"  # 💤 GCP (principal)
  status = 200
  force = true
  # ✅ 2026-05-21 — CORS supprimé (same-origin via proxy, incompatible cookie httpOnly).

[[redirects]]
  from = "/prestataire/*"
  to = "https://api.yukpomnang.com/prestataire/:splat"
  # to = "https://yukpo-fly-backend.fly.dev/prestataire/:splat"  # 💤 Fly direct (sans CF)
  # to = "https://yukpo-backend-376093909298.europe-west1.run.app/prestataire/:splat"  # 💤 GCP (principal)
  status = 200
  force = true
  # ✅ 2026-05-21 — CORS supprimé (same-origin via proxy, incompatible cookie httpOnly).

[[redirects]]
  from = "/user/*"
  to = "https://api.yukpomnang.com/user/:splat"
  # to = "https://yukpo-fly-backend.fly.dev/user/:splat"  # 💤 Fly direct (sans CF)
  # to = "https://yukpo-backend-376093909298.europe-west1.run.app/user/:splat"  # 💤 GCP (principal)
  status = 200
  force = true
  # ✅ 2026-05-21 — CORS supprimé (same-origin via proxy, incompatible cookie httpOnly).

[[redirects]]
  from = "/users/*"
  to = "https://api.yukpomnang.com/users/:splat"
  # to = "https://yukpo-fly-backend.fly.dev/users/:splat"  # 💤 Fly direct (sans CF)
  # to = "https://yukpo-backend-376093909298.europe-west1.run.app/users/:splat"  # 💤 GCP (principal)
  status = 200
  force = true
  # ✅ 2026-05-21 — CORS supprimé (same-origin via proxy, incompatible cookie httpOnly).

[[redirects]]
  from = "/ia/*"
  to = "https://api.yukpomnang.com/ia/:splat"
  # to = "https://yukpo-fly-backend.fly.dev/ia/:splat"  # 💤 Fly direct (sans CF)
  # to = "https://yukpo-backend-376093909298.europe-west1.run.app/ia/:splat"  # 💤 GCP (principal)
  status = 200
  force = true
  # ✅ 2026-05-21 — CORS supprimé (same-origin via proxy, incompatible cookie httpOnly).

[[redirects]]
  from = "/ws/*"
  to = "wss://api.yukpomnang.com/ws/:splat"
  # to = "wss://yukpo-fly-backend.fly.dev/ws/:splat"  # 💤 Fly direct (sans CF)
  # to = "wss://yukpo-backend-376093909298.europe-west1.run.app/ws/:splat"  # 💤 GCP (principal)
  status = 200
  force = true
  # ✅ 2026-05-21 — CORS supprimé (same-origin via proxy, incompatible cookie httpOnly).

[[redirects]]
  from = "/fournitures/*"
  to = "https://api.yukpomnang.com/fournitures/:splat"
  # to = "https://yukpo-fly-backend.fly.dev/fournitures/:splat"  # 💤 Fly direct (sans CF)
  # to = "https://yukpo-backend-376093909298.europe-west1.run.app/fournitures/:splat"  # 💤 GCP (principal)
  status = 200
  force = true
  # ✅ 2026-05-21 — CORS supprimé (same-origin via proxy, incompatible cookie httpOnly).

[[redirects]]
  from = "/echange/*"
  to = "https://api.yukpomnang.com/echange/:splat"
  # to = "https://yukpo-fly-backend.fly.dev/echange/:splat"  # 💤 Fly direct (sans CF)
  # to = "https://yukpo-backend-376093909298.europe-west1.run.app/echange/:splat"  # 💤 GCP (principal)
  status = 200
  force = true
  # ✅ 2026-05-21 — CORS supprimé (same-origin via proxy, incompatible cookie httpOnly).

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# ✅ 2026-05-21 — CORS supprimé des blocs [[headers]] pour /api/*, /auth/*, etc.
# Le backend (Cloudflare → Fly) renvoie déjà ses propres Access-Control-Allow-*
# avec credentials=true et origines whitelist. Doubler ici avec wildcard
# briserait l'auth cookie httpOnly (`withCredentials: true` refuse wildcard).
# Les requêtes vers /api/*, /auth/*, /services/*, etc. sont same-origin via
# le proxy Netlify (status 200), donc CORS pas nécessaire côté Netlify.

[[headers]]
  for = "/*"
  [headers.values]
    # Anti-clickjacking
    X-Frame-Options = "DENY"
    # ✅ 2026-05-21 — X-XSS-Protection RETIRÉ : header déprécié, peut introduire
    # des vulnérabilités sur certains anciens browsers. CSP est la protection
    # moderne. Cf. https://owasp.org/www-project-secure-headers/#x-xss-protection
    # Empêche le MIME-sniffing
    X-Content-Type-Options = "nosniff"
    # HSTS — force HTTPS 2 ans, sous-domaines inclus. Tous les domains Yukpo
    # sont en HTTPS via Netlify/Fly, donc safe à activer.
    Strict-Transport-Security = "max-age=63072000; includeSubDomains; preload"
    # Limite la fuite d'URL référente vers les tiers (privacy + sécurité)
    Referrer-Policy = "strict-origin-when-cross-origin"
    # Restrictions API browser. Autorisé : geo (recherche prox), camera (scan
    # programme/QR), microphone (chat vocal éventuel). Reste désactivé.
    Permissions-Policy = "geolocation=(self), camera=(self), microphone=(self), payment=(), usb=(), magnetometer=(), gyroscope=(), accelerometer=()"
    # Isolation cross-origin (limite Spectre + clic-jacking via popups)
    Cross-Origin-Opener-Policy = "same-origin-allow-popups"
    # ✅ 2026-05-21 — CSP MODE ENFORCING (plus en Report-Only).
    # Bloque réellement toute exécution/exfiltration interdite.
    # Tightening vs version précédente :
    #   • script-src : 'unsafe-eval' RETIRÉ (n'est plus nécessaire en build prod)
    #   • connect-src : 'https:' wildcard RETIRÉ — liste exacte de domaines
    #     pour bloquer toute exfiltration XSS vers un domaine attaquant
    #   • upgrade-insecure-requests : force HTTPS sur toute ressource
    #   • Hosts API listés explicitement (Cloudflare/Fly direct/GCP fallback)
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-inline' https://maps.googleapis.com https://maps.gstatic.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://maps.googleapis.com; img-src 'self' data: blob: https:; font-src 'self' data: https://fonts.gstatic.com; connect-src 'self' https://api.yukpomnang.com wss://api.yukpomnang.com https://yukpo-fly-backend.fly.dev wss://yukpo-fly-backend.fly.dev https://yukpo-backend-376093909298.europe-west1.run.app wss://yukpo-backend-376093909298.europe-west1.run.app https://maps.googleapis.com https://*.tile.openstreetmap.org https://photon.komoot.io https://api.openai.com; frame-ancestors 'none'; base-uri 'self'; form-action 'self'; object-src 'none'; upgrade-insecure-requests"
