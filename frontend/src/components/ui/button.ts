// Alias pour compatibilité — le dossier réel est ./buttons
export { Button, PrimaryButton, DangerButton, OutlineButton, GhostButton, LoadingButton, IconButton, SecondaryButton } from './buttons';
export type { ButtonProps } from './buttons';
