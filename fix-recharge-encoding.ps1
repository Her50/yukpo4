$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green

$filePath = "mobile\src\screens\RechargeTokensScreen.tsx"
$content = Get-Content $filePath -Raw -Encoding UTF8

# Corriger les problèmes d'encodage
$content = $content -replace 'prdfinies', 'prédéfinies'
$content = $content -replace 'Mthodes', 'Méthodes'
$content = $content -replace "Instantan'", "Instantané'"
$content = $content -replace 'slectionner', 'sélectionner'
$content = $content -replace 'mthode', 'méthode'
$content = $content -replace 'cre', 'créé'
$content = $content -replace 'Succs', 'Succès'
$content = $content -replace 'Dtails', 'Détails'
$content = $content -replace 'Slectionner', 'Sélectionner'
$content = $content -replace 'Slectionnez', 'Sélectionnez'

# Sauvegarder le fichier
Set-Content $filePath -Value $content -Encoding UTF8 -NoNewline

Write-Host "✅ Encodage corrigé dans RechargeTokensScreen.tsx" -ForegroundColor Green














